#include "buzz.h"

void testBuzzFunctions(); // Function prototype

void setup() {
    Serial.begin(115200);
    delay(1000); // Wait for serial to initialize
    Serial.println("Starting buzz function tests...");
}

void loop() {
    testBuzzFunctions();
    delay(5000); // Wait 5 seconds before repeating the tests
}

void testBuzzFunctions() {
    // Serial.println("Testing buzz functions...");

    // Serial.println("Single short buzz:");
    // shortBuzz();
    // delay(5000);

    Serial.println("Double buzz:");
    doubleBuzz();
    delay(50);

    // Serial.println("Connected buzz:");
    // connectedbuzz();
    // delay(500);

    // Serial.println("Full buzz:");
    // buzz();
    // delay(500);

    Serial.println("Buzz functions test completed.");
}
